FROM nginx:alpine

# Copy Laravel app public assets
COPY ./authapi/public /var/www/html/public

# Copy custom NGINX config
COPY ./nginx/nginx-kubernetes.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]